import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 20
    },
    currentPrice: {
        color: "#fff",
        fontSize: 32,
        fontWeight: "bold",
        paddingTop: 20,
    },
    textPrice: {
        color: "#ddd",
        fontSize: 16,
        fontWeight: "bold"
    }
});

export default style;